// in js only function name must be match not require 
// to match number of parameter as well as type of 
// parameter 
function add(a,b){
    console.log(a+" "+b);
}
add(10,20);
add("A","B");
add(1);
add();